# MovieMatrix
MovieMatrix is a full-stack movie discovery web app built with React, Tailwind CSS, Spring Boot, and MySQL. Users can browse movies fetched from an external API, view details, rate films, and manage watchlists. It includes JWT authentication and separate admin and user interfaces with CRUD operations.
